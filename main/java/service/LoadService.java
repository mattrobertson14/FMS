package service;

import regRes.LoadRequest;
import regRes.LoadResponse;

public class LoadService {

    /**
     * The Handler will call this to try and clear the DB and load new data in
     * @param request
     * @return a LoadResponse
     */
    public LoadResponse load(LoadRequest request){
        return null;
    }
}
